{
  "name": "routingall",
  "version": "1.6.0",
  "description": "ROUTINGALL — Lightweight Local Developer Proxy & Master-Key Gateway",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
